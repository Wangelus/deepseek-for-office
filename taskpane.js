/* ============================================
   DeepSeek for Office — 入口
   装配并启动应用；全部模块逻辑见 src/
   ============================================ */

import { App } from './src/App.js';

App.create().start();
